"""The medical-appointment solution.

Kept separate from the official harness (api.py, dtos.py, utils.py,
local_evaluator.py) so upstream updates to those files never conflict with
our own code. example.py is the only official file that imports from here.
"""
